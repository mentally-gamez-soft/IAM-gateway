```markdown
# Task 019-1 — Add `coverage` to Project Dependencies

## Parent User Story

[US-019 — Test Coverage Measurement](../USER_STORIES/user_story_019.md)

## Description

Add the `coverage` library to the project's dependency files so it is available in the development environment.

## Work to be Done

1. Add `coverage>=7.0` to `requirements.in`
2. Add `coverage>=7.0` (with its pinned version) to `requirements.txt`
3. Add `coverage>=7.0` to the `dependencies` list in `pyproject.toml`

## Expected Outcome

`coverage` is installable via `uv pip install -e .` or `pip install -r requirements.txt` without any additional manual steps.

## Priority

**Medium**

## Acceptance Criteria

- [ ] `coverage` is present in `requirements.in`
- [ ] `coverage` is present in `requirements.txt`
- [ ] `coverage` is present in `pyproject.toml` dependencies
- [ ] `uv run coverage --version` succeeds in the project virtual environment

## Status

**0%**

## Trello

[TASK-019-1 — Add coverage to project dependencies](https://trello.com/c/raXSqudP)
```
