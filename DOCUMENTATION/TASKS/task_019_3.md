```markdown
# Task 019-3 — Update `.gitignore` for Coverage Artefacts

## Parent User Story

[US-019 — Test Coverage Measurement](../USER_STORIES/user_story_019.md)

## Description

Ensure that coverage-generated artefacts (`htmlcov/` directory and `.coverage` data file) are not committed to the repository.

## Work to be Done

1. Add `htmlcov/` to `.gitignore`
2. Add `.coverage` to `.gitignore`
3. Add `.coverage.*` to `.gitignore` (for parallel coverage data files)

## Expected Outcome

Running `coverage html` does not produce any untracked files that would appear in `git status` after the entries are added to `.gitignore`.

## Priority

**Low**

## Acceptance Criteria

- [ ] `htmlcov/` is listed in `.gitignore`
- [ ] `.coverage` is listed in `.gitignore`
- [ ] After running `coverage html`, `git status` shows no new untracked files related to coverage

## Status

**0%**

## Trello

[TASK-019-3 — Update .gitignore for coverage artefacts](https://trello.com/c/81II8cKB)
```
